plugins {
    id 'application'
}

dependencies {
    implementation project(':ui')
}

application {
    mainClass = "${rootProject.property('namespace')}.cli.Cli"
}

run {
    workingDir = file('run').tap { mkdirs() }
}
