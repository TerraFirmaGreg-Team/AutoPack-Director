plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'AutoPack-Director'

['core', 'standalone', 'launchwrapper', 'modlauncher', 'universal'].each { name ->
    include name
    project(":$name").projectDir = file("modules/$name")
}
